<div class="row alert alert-warning">
	<!-- Child name -->
	<div class="col-md-4">
            <i class="fa fa-star"></i>
	</div>
	
	<!-- Child meters -->
	<div class="col-md-5">
		<h4>&nbsp; {{ action.actions.name }}</h4>
	</div>
	
	<!-- Child points -->
	<div class="col-md-3">
		<h4>&nbsp; {{ (action.acumulated * action.actions.points)|round(0,'ceil')}} {{ 'Points.name'|trans }} {{ 'Points.historical.exchange'|trans }}</h4>
	</div>
</div>