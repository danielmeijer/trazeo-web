<script type="text/javascript">
	var analytics_user=analytics.identify('{{ app.user.id }}',{email: '{{ app.user.email }}', username: '{{ app.user }}'});
	//Analytics invite group button 	
	$(".howto").click(function (event){
		analytics.track('click.points.howto'); 
	});	
	$(".historical").click(function (event){
		analytics.track('click.points.historical'); 
	});	
</script>