{% extends 'TrazeoFrontBundle:Panel:base.html.twig' %}

		{% block body_content %}
		
			<div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">
                    {{ "Panel.title" | trans  }}
                    </h1>
                </div><!-- /.col-lg-12 -->
		{% endblock %}
		
{% block body -%}
    <h1>UserExtend list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nick</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('panel_userextend_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nick }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('panel_userextend_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('panel_userextend_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('panel_userextend_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
		