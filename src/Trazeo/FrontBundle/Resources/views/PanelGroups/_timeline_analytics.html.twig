<script type="text/javascript">
	var analytics_user=analytics.identify('{{ app.user.id }}',{email: '{{ app.user.email }}', username: '{{ app.user }}'});
	//Analytics invite group button 	
	$(".input-group-btn").click(function (event){
		analytics.track('click.timeline.invite.group',{group: '{{ group.name }}'}) 
	});
	//Analytics post timeline button 	
		$('.fos_comment_submit > input').click(function (event){
		analytics.track('click.timeline.post',{group: '{{ group.name }}'}) 
	});
	//Analytics child join group
	var link=$('.join');
	analytics.trackLink(link,"click.timeline.join",{group: '{{ group.name }}'});	
	//Analytics new child button
	var link=$('.new_child');
	analytics.trackLink(link,'click.child.prev.new');	
</script>