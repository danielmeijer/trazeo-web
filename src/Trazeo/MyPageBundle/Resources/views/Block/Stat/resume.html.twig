{% extends sonata_block.templates.block_base %}

{% block block %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">{{ settings.title }}</h3>
        </div>
        <div class="box-body">
            <a href="{{ path('registered_evolution') }}">Evolución de familias registradas y niños que participan</a>
        </div>
    </div>
{% endblock %}