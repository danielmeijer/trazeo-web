{% extends sonata_block.templates.block_base %}

{% block block %}
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">{{ settings.title }}</h3>
        </div>
        <div class="box-body">
            <ul>
                <li>
                    <a href="{{ path('moduleComposer_view') }}">
                        {{ 'compose_module'|trans }}
                    </a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}