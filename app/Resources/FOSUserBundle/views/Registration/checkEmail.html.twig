{% extends 'SopinetTemplateAceBundle:Base:god.html.twig' %}

{% block title %}Login{% endblock %}

{% block custombody %}<body class="login-layout">{% endblock %}
{% block body %}
    <div class="container-fluid" id="main-container">

		<div class="container-fluid" id="main-container">
			<div id="main-content">
				<div class="row-fluid">
					<div class="span12">
						<div class="login-container">
							<div class="row-fluid">
								<div class="center">
									<h1>
										<img src="{{ asset('bundles/suspasitosfront/images/home/jazzy-logo-glow.png') }}" alt="" />
										<!-- <i class="icon-leaf green"></i>
										<span class="red">Sus</span>
										<span class="white">Pasitos</span>-->
									</h1>
								</div>
							</div>

							<div class="space-6"></div>

							<div class="row-fluid">
								<div class="position-relative">

    
{#    
	      {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
	      	<div class="pull-right">
	          {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
	          <a href="{{ path('home') }}">Acceder</a> | 
	          <a href="{{ path('fos_user_security_logout') }}">
	              {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
	          </a>
	        </div>
	      {% endif %}
#}
								{#
									<!-- getFLASHES ERROR, REVISAR!-->
									{{ ladybug_dump(app.session.flashbag) }}
								{% for key, message in app.session.flashbag.get('error') %}
								<div class="{{ key }}">
								    {{ message|trans({}, 'FOSUserBundle') }}
								</div>
								{% endfor %}
								#}
								
								{% for type, flashMessages in app.session.flashbag.all() %}
								    {% for flashMessage in flashMessages %}
								        <div class="alert alert-{{ type }}">
								   			<p>{{ 'registration.check_email'|trans({'%email%': user.email}, 'FOSUserBundle') }}</p>
								        	<a href="{{ path('fos_user_security_login') }}"><input type="button" class="btn" value="Ir a login"></a>
								        </div>
								    {% endfor %}
								{% endfor %}							

      
								</div>
							</div>
						</div>
					</div><!--/span-->
				</div><!--/row-->
			</div>
		</div><!--/.fluid-container-->      		

    </div>
{% endblock %}

{% block js %}
	{{ parent() }}
	<script type="text/javascript">
		function show_box(id) {
		 $('.widget-box.visible').removeClass('visible');
		 $('#'+id).addClass('visible');
		}
	</script>	
{% endblock %}